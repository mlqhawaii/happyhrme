Map marker interaction update:
- Clicking any venue marker moves that venue to the first row of the results list.
- The row is briefly highlighted.
- If active filters hide the clicked venue, those filters are relaxed so the venue can be shown.
- Existing verified/no-happy-hour/checking status order remains unchanged until a marker is explicitly selected.
